[tool.black]
line-length = 79

[tool.poetry]
name = "incode"
version = "0.4.0"
description = "Code injector for automating configuration, setup, and yak shaving."
readme = "README.rst"
repository = "https://github.com/tonysyu/incode"
documentation = "https://incode.readthedocs.io/"
authors = ["Tony S. Yu <tsyu80@gmail.com>"]
license = "BSD-3-Clause"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Topic :: Software Development",
    "License :: OSI Approved :: BSD License",

    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.6",
    "Programming Language :: Python :: 3.7",
]
keywords = ["console", "terminal"]

[tool.poetry.dependencies]

sphinx = { version = "<2", optional = true }
sphinx-autobuild = { version = "*", optional = true }
sphinxcontrib-napoleon = { version = "*", optional = true }

[tool.poetry.dev-dependencies]
bumpversion = "*"
coverage = "*"
flake8 = "*"
mock = "*"
pytest = "*"
pytest-cov = "*"
pytest-flake8 = "*"
pytest-sugar = "*"
twine = "*"
wheel = "*"
pre-commit = "^1.17"

[tool.poetry.extras]
docs = ["sphinx", "sphinx-autobuild", "sphinxcontrib-napoleon"]

[tool.poetry.scripts]
incode = "incode.cli:main"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
