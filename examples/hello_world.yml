operations:
    prompt_user:
        local_context:
            prompts:
                - name: "name"
                  default_value: "World"
    echo:
        local_context:
            message: |

                Hello {{ qwikstart.name }}!

                {{ 'Welcome to the qwikstart tool!' | colored("yellow") }}
