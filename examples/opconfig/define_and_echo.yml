steps:
    "Define name to greet":
        name: define_context
        context_defs:
            template_variables:
                name: "World"
    "Display greeting":
        name: echo
        message: |
            Hello, {{ qwikstart.name }}!
