operations:
    prompt_user:
        inputs:
            - name: "name"
              default: "World"
            - name: "target_dir"
              default: "."
    define_context:
        context_defs:
            target_path: "{{ qwikstart.target_dir }}/hello_world_with_prompt.txt"
    add_file:
        template_path: "./templates/hello_world.txt"
