steps:
    "Add redis to docker-compose file":
        name: edit_yaml
        file_path: "examples/data/docker-compose.yml"
        merge_data:
            services:
                redis:
                    image: "redis:alpine"
